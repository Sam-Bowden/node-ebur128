{
	"name": "node-ebur128",
	"version": "0.1.0",
	"description": "Node.js native addon wrapping the Rust ebur128 crate",
	"scripts": {
		"build": "napi build -r --manifest-path Cargo.toml -o dist"
	},
	"devDependencies": {
		"@napi-rs/cli": "^3.5.1"
	}
}
